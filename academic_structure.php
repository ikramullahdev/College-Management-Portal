<?php session_start(); require 'config/database.php'; require 'lib_academic.php'; if(!isset($_SESSION['user'])||$_SESSION['user']['role']!=='admin'){header('Location: login.php');exit;}
$groups=['HSSC'=>[], 'BS'=>[], 'ADP'=>[]];$programs=$pdo->query('SELECT * FROM programs ORDER BY FIELD(level,"HSSC","BS","ADP"),name')->fetchAll();foreach($programs as $p)$groups[$p['level']][]=$p;
$pageTitle='Academic Structure';require'includes/header.php';?>
<div class="container py-4"><div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="fw-bold">Academic Structure</h2><p class="text-muted mb-0">Standard term structure for HSSC, BS and ADP programs.</p></div><a href="admin.php" class="btn btn-outline-secondary">Dashboard</a></div>
<div class="row g-4"><div class="col-lg-4"><div class="card shadow-sm border-0 h-100"><div class="card-body"><span class="badge text-bg-primary">HSSC</span><h4 class="mt-2">Higher Secondary</h4><ul class="list-group list-group-flush"><?php foreach(academicTerms('HSSC') as $t):?><li class="list-group-item px-0"><?=$t?></li><?php endforeach;?></ul><hr><strong>Programs</strong><?php foreach($groups['HSSC'] as $p):?><div class="small mt-2">• <?=htmlspecialchars($p['name'])?></div><?php endforeach;?></div></div></div>
<div class="col-lg-4"><div class="card shadow-sm border-0 h-100"><div class="card-body"><span class="badge text-bg-success">BS</span><h4 class="mt-2">4-Year Degree</h4><div class="row g-2"><?php foreach(academicTerms('BS') as $i=>$t):?><div class="col-6"><div class="border rounded p-2 text-center small"><?=$t?></div></div><?php endforeach;?></div><hr><strong>Programs</strong><?php foreach($groups['BS'] as $p):?><div class="small mt-2">• <?=htmlspecialchars($p['name'])?><?php if($p['specialization']):?> — <?=htmlspecialchars($p['specialization'])?><?php endif;?></div><?php endforeach;?></div></div></div>
<div class="col-lg-4"><div class="card shadow-sm border-0 h-100"><div class="card-body"><span class="badge text-bg-warning">ADP</span><h4 class="mt-2">Associate Degree</h4><div class="row g-2"><?php foreach(academicTerms('ADP') as $t):?><div class="col-6"><div class="border rounded p-2 text-center small"><?=$t?></div></div><?php endforeach;?></div><hr><strong>Programs</strong><?php foreach($groups['ADP'] as $p):?><div class="small mt-2">• <?=htmlspecialchars($p['name'])?></div><?php endforeach;?></div></div></div></div>
<div class="alert alert-info mt-4 mb-0"><strong>Note:</strong> The term labels are portal structure defaults. Official examination rules, promotion criteria and grading policy should be configured according to the applicable board/university regulations before production use.</div></div><?php require'includes/footer.php';?>
